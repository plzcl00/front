# Copy to .env for local npm run dev or docker compose service `front`.
VITE_API_URL=http://localhost:8080

# If you still see CORS errors in the browser, open the app via http://localhost:5173
# (not 127.0.0.1) or set APP_CORS_ALLOWED_ORIGIN_PATTERNS on the backend to include your origin.
